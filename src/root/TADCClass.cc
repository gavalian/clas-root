/**
* Autogenerated Source File for TADCClass
* \\file TADCClass.cc
* \\author G.Gavalian
* \\date 2013-11-13
*/
#include "TADCClass.h"


ClassImp(TADCClass)

TADCClass::TADCClass()
{
}

TADCClass::TADCClass(int nrows){
  Init(nrows);
}

TADCClass::TADCClass(int slot, int channel,int nrows)
{
  Init(slot,channel,nrows);
}

TADCClass::TADCClass(const TADCClass &obj)
{
  slot_number    = obj.slot_number;
  channel_number = obj.channel_number;
  adcValue.Set(obj.adcValue.GetSize());
  //printf("[copy constructor] ----> %d %d %d\n",slot_number,channel_number,obj.channel_number);
  for(int loop = 0; loop < adcValue.GetSize();loop++)
    {
      adcValue.AddAt(obj.adcValue.At(loop),loop);
    }
}

TADCClass::~TADCClass()
{
  
}

void TADCClass::Init(int nrows)
{
  adcValue.Set(nrows);
}

void TADCClass::SetSlotChannel(int slot, int channel)
{
  slot_number    = slot;
  channel_number = channel; 
}

Short_t TADCClass::GetValue(int row)
{
  return adcValue.At(row);
}
void TADCClass::SetValue(int row, int value)
{
  adcValue.AddAt((Short_t) value,row);
}

void TADCClass::Init(int slot, int channel, int nrows)
{
  SetSlotChannel(slot,channel);
  Init(nrows);
}

/* const TADCClass &TADCClass::operator=(const TADCClass &obj){} */
